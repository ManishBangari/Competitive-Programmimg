// to find the permutations of a given array

#include<iostream>
#include<vector>

using namespace std;

void findPermutations(vector<int>& vec, vector<int>& perms){
    
}

int main(){

    vector<int> vec{1,2,3};
    vector<int> perms;

    findPermutations(vec, perms);
    return 0;
}